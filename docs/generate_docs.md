# Інструкція з генерації документації

## Інструментарій
Використовується пакет `pdoc` для автоматичного витягування Docstrings.

## Генерація документації
Використовується пакет `pdoc`. Команда для генерації:
`pdoc -o docs/api_docs backend/main.py`